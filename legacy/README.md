# Frozen legacy implementation

`pyLeaf.py` is copied byte for byte from upstream pyLeaf commit
`e96a34cae637687e4f6d4e0d55aaa27bf4381aa3`. It is retained only as the comparison
baseline and must not be edited.

Files:

- `pyLeaf.py`: current NumPy/GEKKO model
- `Input.xlsx`: current sample forcing workbook
- `test_Simulation.py`: current plotting demonstration
- `README.original.md`: current upstream documentation

The protected SHA-256 for `pyLeaf.py` is:

```text
d14cd68d3ae90dcfd2d993ef379b4ef7a06f21b55941ad801fa3e95e55797efd
```

Run the repository-level `examples/compare_models.py` instead of editing this
snapshot.
